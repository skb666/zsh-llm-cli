# omp backend: tag prefix 💠[omp]
# Default argv: `omp -c -p --thinking auto <prompt>` (mirror of the legacy
# zsh-omp-cli plugin's default invocation).

[backend]
name = omp
prefix = 💠
bin = omp

[args]
always = -c
always = -p
always = --thinking auto

[stream]
use_sd = auto
