# Default global config for zsh-llm-cli. User overrides go to
# ~/.config/zsh-llm-cli/config.ini (whole-file override).

[ui]
rprompt_format = llm-cli: %name %tag

[cycle]
key_toggle = ^X
key_next = ^N
